# Clockwise

Clockwise is a time management dashboard application.

## Versioning

Releases use semantic versions such as `1.2.3`. Pushes and pull requests run verification builds without publishing. Releases are started manually from the GitHub Actions `release-dashboard` workflow by entering the desired version.
